import { Box } from "@mui/material";
import Counter from "components/Effect/Counter";
import React from "react";

const Effect = () => {
  return <Box display="grid" gap={5}>
    <Counter />
  </Box>;
};

export default Effect;
